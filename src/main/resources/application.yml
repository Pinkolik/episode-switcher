keycloak:
  auth-server-url: https://episode-switcher.ru/auth
  realm: EpisodeSwitcher
  resource: episode-switcher
  bearer-only: true
server:
  port: 8080
spring:
  jackson:
    property-naming-strategy: SNAKE_CASE
    default-property-inclusion: non_null